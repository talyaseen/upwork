# Minimal dependencies to run the test suite WITHOUT downloading the heavy
# sentence-transformers / torch stack. The suite uses the deterministic "hash"
# embedding backend, so the real model is not required for tests.
fastapi==0.115.6
pydantic==2.10.4
pydantic-settings==2.7.0
SQLAlchemy==2.0.36
aiosqlite==0.20.0
python-multipart==0.0.20
PyJWT==2.10.1
bcrypt==4.2.1
numpy==2.2.1
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
asgi-lifespan==2.1.0
