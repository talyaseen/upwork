---
galaxy_info:
  author: Talal Alyaseen
  license: MIT
  role_name: edge_proxy
  description: >-
    edge reverse-proxy role: nginx behind Cloudflare (Origin cert, Full strict,
    Authenticated Origin Pulls) with TWO vhosts - a pure-static apex portfolio,
    and a chat demo serving the static Flutter build plus /api reverse-proxied to
    the hub over a 1:1 mutual-TLS leg. Security headers + gzip. No certbot.
  min_ansible_version: "2.14"
dependencies: []
